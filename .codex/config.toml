# ============================================================
# Codex Configuration File
# ============================================================

# Root directory Codex is allowed to access
workspace = "/workspaces/agenticsa"

# Whether Codex may execute shell commands
allow_shell = true

# ============================================================
# MCP (Model Context Protocol) Configuration
# ============================================================

[mcp]
# Enable MCP support
enabled = true

# Default timeout (in milliseconds) for MCP calls
timeout_ms = 30000

# ============================================================
# MCP Servers
# ============================================================

[mcp.servers]
[mcp_servers.cpptest-sa]
name = "cpptest-sa"
description = "Static analysis data"
transport = "stdio"
command = "/opt/parasoft/cpptest/integration/mcp/cpptestmcp"
args = []

# ============================================================
# Security and Capability Controls
# ============================================================

[mcp.capabilities]
# Allow MCP servers to return large payloads
max_response_bytes = 10_000_000


# ============================================================
# Logging
# ============================================================

[logging]
level = "info"
file = "/home/user/.codex/logs/codex.log"
